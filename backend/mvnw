#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
MAVEN_HOME="$SCRIPT_DIR/.mvn/apache-maven-3.9.8"
if [ ! -x "$MAVEN_HOME/bin/mvn" ]; then
  echo "ERROR: Local Maven distribution not found at $MAVEN_HOME" >&2
  exit 1
fi
exec "$MAVEN_HOME/bin/mvn" "$@"
